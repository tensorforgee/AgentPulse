# Server-only URL used by Next.js Route Handlers to reach the AgentPulse API.
# In production, use the API's private service URL when the platform provides one.
AGENTPULSE_API_URL=http://127.0.0.1:5000
